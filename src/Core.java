public class Core extends PhysicalEntity {

    /**
     * Creates a new Core with a given size.
     * 
     * @param sizeRadius
     *            The given size.
     */
    public Core(float sizeRadius) {
	super(0, sizeRadius, 0, 0);
    }
}
